# Restaurant Recommendation System

나는 음식에 진심이다!.

## 설치 및 실행 방법

### 1. 환경 변수 설정

프로젝트 루트 디렉토리(`restaurant-recommendation-system`)에 `.env.local` 파일을 생성하고 하단 내용을 복사 붙여넣기 하여 설정합니다.

```bash
VITE_GOOGLE_PLACES_API_KEY=AIzaSyBX_w6BirTiZLS7lwgvfgtAbvA7PBwHDQ4
```

### 2. 의존성 패키지 설치

프로젝트 디렉토리에서 통합 터미널을 열고 다음 명령어를 실행합니다:

```bash
npm install
```

### 3. 프로젝트 실행

설치가 완료되면 다음 명령어로 개발 서버를 실행합니다:

```bash
npm start
```
